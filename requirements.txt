# Streamlit app only — no instagrapi/pydantic to avoid Python 3.14 build failures
# tweepy is intentionally excluded: tweepy 4.14 uses 'imghdr' which was removed
# in Python 3.13+. X posting on Streamlit uses requests-oauthlib instead.
streamlit>=1.33.0
supabase>=2.4.3
requests-oauthlib>=1.3.1
python-dotenv>=1.0.1
requests>=2.31.0
